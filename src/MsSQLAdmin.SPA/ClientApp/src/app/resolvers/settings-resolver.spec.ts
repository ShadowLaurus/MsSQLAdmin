import { SettingsResolver } from './settings-resolver';

describe('AppSettingsResolver', () => {
  it('should create an instance', () => {
    expect(new SettingsResolver(null)).toBeTruthy();
  });
});
